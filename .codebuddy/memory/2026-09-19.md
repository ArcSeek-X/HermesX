# 2026-09-19

## 测试文件审计与 basic/__tests__ 补充

用户两项要求：
1. 审计 `apps/hrs-web/src` 下测试文件是否与工程匹配（合理/规范/正确/及时）。
2. 补充 `apps/hrs-web/src/components/basic/__tests__/`（用户以为缺，其实已有 Button/Input/PasswordInput 三个，是我上一轮把组件从 common 迁到 basic 时一并迁来的）。

### 全量基线（npx vitest run）
- 105 个测试文件：**87 通过 / 18 失败**。
- 18 个失败文件归类：
  - **A. 表单 label 未与控件关联（getByLabelText 取不到）**：`AlertRuleForm.test`、`AlertRuleList.test`、`StockSearch.test`（accessible label 相关）。
  - **B. 页面/路由测试时序与 mock**：`App.test`、`HomePage`、`PortfolioPage`、`SettingsPage`、`StockKLinePage.functional`、`StockScreeningPage`、`TokenUsagePage`、`DecisionSignalsPage` —— 多为 `not wrapped in act(...)`（异步更新未包 waitFor/act）或缺失 Router/mock。
  - **C. 主题/治理类过时断言**：`tests/index.theme-bootstrap.test`（断言 index.html 含 `const storageKey='theme'`，与实际不符）、`tests/login-theme-tokens.test`、`tests/ui_governance.test`。

### 已修复（本次）
- `basic/__tests__/` 新增 4 个套件：Select / Checkbox / Switch / TextArea；现共 7 文件 38 用例全绿。
- `AlertsPage.test.tsx` 8/8 全绿。根因：① 表单 label 未关联 input（修 AlertRuleForm 加 htmlFor/id）；② 原生 `<select>` 被测试按 `getByRole('button')` 查（应为 combobox，改用 `getByDisplayValue`）；③ `listRules.mockReset()` 污染后续用例（改为 `mockReturnValueOnce` 不 reset）。

### 关键约定/坑（供后续）
- `basic/Select.tsx` 是原生 `<select>` + 独立 `<label>`，**无 htmlFor/id**；`basic/Input.tsx` 同样标签外置。项目大量表单用 `<label>文字</label><Input/>` 而不关联，导致 `getByLabelText` 失效——这是测试失败的主类。
- 记忆规则「输入框改动只动 Input.tsx」约束：label 关联这类 a11y 修复若改到 Select/AlertRuleForm 等，属边界，本次为修复 AlertsPage 测试已对 AlertRuleForm 加过 htmlFor；其余同类失败优先用「测试侧改查询方式（role/displayValue）」避免动生产组件。
- `basic/InputTest.tsx` 是被 `Index.ts` 导出的「实验性/疑似重复」Input 实现（与 Input.tsx 并存），未被任何内部组件引用——疑似遗留，建议后续确认是否清理。
