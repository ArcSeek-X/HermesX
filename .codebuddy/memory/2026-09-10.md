# 2026-09-10

## 组件目录迁移（common 下）
- 将 `components/common/ListCard.tsx` 移入 `components/common/Card/`（新建 `Card/ListCard.tsx` + 在 `Card/index.ts` 补 `ListCard` 导出），删除旧平铺文件。关键点：原文件 `import { Chip, HrsButton } from '../'` 指向 `components/`，移入子目录后必须改成 `'../../'`，`cn` 改 `../../../utils/cn`；3 处调用方（Docs_listCard、WatchlistGroupPanel、SettingsCategoryNav）改引用 `../common/Card`。eslint 0 报错。
- 将 `components/common/TabNav.tsx` 移入 `components/common/TabNav/`（新建 `TabNav/TabNav.tsx` + `TabNav/index.ts`，导出 TabNav/TabNavItem/TabNavProps），删除旧平铺文件。只需改 `cn` 路径 `../../../utils/cn`。`common/Index.ts` 已有 `export * from './TabNav'`、ThemeSetting 用目录路径，调用方无需改。eslint 仅余 SectorAnalysisPage/StockDashboardPage 的既有问题（与本次无关）。

## 前后端启停
- 杀掉项目前后端进程（uvicorn / vite / main.py / webui.py，端口 8000/5173 兜底），并重新启动：后端 `python -m uvicorn server:app --reload --port 8000`（根目录，需 `source .venv`）；前端 `npm run dev`（apps/hrs-web，需 nvm v20）。
- 发现 `vite.config.ts` 的 `server.port` 实际是 **1022**（非 LOCAL_RUN_GUIDE 文档里写的 5173），前端监听 1022，`/api` 代理到 127.0.0.1:8000。启动后验证：前端根 200、/api 代理 200、后端 /docs 200，联调正常。
- 注意：`execute_command` 后台启动需用 `nohup ... &`（子 shell），否则命令被识别为 watch 在 10s 后提前返回、sleep 验证不执行；nohup 进程本身可常驻。

## 未完成
- 生成 `Docs_checkbox` 文档页（路由 /docs/component/checkbox，菜单「复选框」，文件 `apps/hrs-web/src/pages/DocsPage/Components/Docs_checkbox.tsx`）任务被中途打断，尚未创建页面文件，todo 仍 pending。
