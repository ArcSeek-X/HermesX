# 2026-09-07

- 消息日历点击语义重构（方案 v2 实施完成）：新建 `LiveCalendarEventDrawer.tsx`（底座 `components/basic/Drawer` 的 HrsDrawer，size=lg），点消息在月 / 周 / 日三视图**统一只开抽屉、不切视图**；切日视图仅由「点日期格空白 / 日期标题」触发（handleDateClick → goToDayView）。删除 handleEventClick 与 GroupedEventContent 内的 changeView。Page 新增 selectedEvent state + 挂载抽屉、删除 EventCard 内 console.log、移除不再使用的 toDateKeyFromSeconds 导入。i18n 三语各补 8 个 key。tsc 全量通过，文档 §9 / §11 / §19 / §20 交互表已同步。
- 待办（用户后续会提）：删除日历下方「当天汇总」详情面板（AnimCard + selectedDay 相关逻辑），切日视图保留。精简文件头（补 List 视图说明）、删除多处修改过程产物（jumpToDayView 移除 / List 标题移走 / LiveCalendarPage 接管按钮等）、合并与代码及彼此重复的冗余说明（eventBaseTops、eventDidMount 首帧 NaN 等）、修正 moreLink 笔误。lint 0 错误，未改动任何运行逻辑。
- 详情抽屉改为用户指定的三段式布局：Header=时间（年月日+时分）+主标题 shortTitle；Body=国家徽章（国旗+名称+货币 currency）+分类 chip → 副标题 title → 正文 summary（Body 可滚动）→ 经济数据（仅 FD 且有值）；Footer=重要度（左，色点+文案）+来源外链（右）。要点：货币取自国家字典 `useLiveCalendarCountries().items`，由 Page 以 `countries` prop 传入（不在抽屉内重复请求）；来源名用 i18n `component.LiveCalendar.drawer.sourceName`（华尔街见闻 / 華爾街見聞 / Wallstreetcn）而非域名。tsc 通过。
- 新增基础组件 `apps/hrs-web/src/components/basic/Drawer.tsx`：基于 HeroUI v3（3.2.4）Drawer 二次封装的侧滑弹窗，导出名 `HrsDrawer`（避免与已存在的 `components/common/Drawer.tsx` 在 `components/index.ts` 处重名冲突）。声明式（isOpen/onClose + Header/Body/Footer children 分拣）+ 复合组件（Object.assign 挂载 HeroUI 原生插槽）双模式；补齐 HeroUI 原生没有的 size 档位（左右控宽、上下控高），遮罩渐变沿用项目 Modal 方案。已在 `components/basic/index.ts` 注册，tsc --noEmit 与 eslint 均通过。
- 环境事实：HeroUI 组件样式位于 `@layer components`，Tailwind utilities 层可覆盖；覆盖左右方向宽度时必须显式给 `sm:` 断点类，否则会被 HeroUI 基础样式里的 `sm:w-96` 压过。
- `apps/hrs-web/src/pages/CodeTestPage.tsx` 顶部新增 HrsDrawer 演示区：placement / size / variant 三组单选按钮 + showHandle / hideCloseButton / isDismissable 三个开关 + 声明式用法（title + Body 30 条长列表验证滚动 + Footer 双按钮）。tsc --noEmit 与 eslint 均通过。
- HrsDrawer 响应式尺寸策略重构（用户提出「窄屏时按 vw 算宽度没意义，高度敏感度更低」）：宽度改用「阈值+降档」——< sm(640px) 铺满 `w-full`，≥ sm 紧凑宽度，≥ md 标准宽度，统一 `max-w-[calc(100%_-_3rem)]` 兜底（固定留白保证遮罩可点）；高度保留 dvh 比例但收口为 `max-h-[min(100dvh,max(最小可用高,Xdvh))]`。抽 `isVerticalPlacement()` 判定函数。
- HrsDrawer size 扩展为七档：新增 xs / xxl。水平方向 xs `sm:w-56 md:w-64`、xxl `sm:w-[28rem] md:w-[48rem]`；垂直方向 xs `max-h-[min(100dvh,max(10rem,25dvh))]`、xxl `max-h-[min(100dvh,max(36rem,95dvh))]`。`CodeTestPage.tsx` 的 size 选项同步补齐。已用 Tailwind CLI 验证新类均被生成。
- 环境事实补充：HeroUI 样式在 `@layer components`，级联层优先级高于选择器特异性与源序，因此 utilities 层的无前缀类（如 `w-64`）就能覆盖 HeroUI 的 `sm:w-96`，无需写同名断点类。Tailwind CLI（`npx @tailwindcss/cli -i src/style/index.css -o /tmp/x.css`）可用于验证任意值类是否被生成；用 python regex 从产出 CSS 提取带转义的类名最可靠。
- 为 `hooks/useLiveCalendar.ts` 补充注释：三个 Hook 返回值类型字段注释（含 `degraded` 降级标记）、`useLiveCalendarMonths` 内部 useState/abortRef/loading 计算，以及 return 语句 8 个返回值逐行注释。纯注释补充，未改动运行逻辑，lint 0 错误。
- `LiveCalendarEventDrawer.tsx` 标题换行修复：外层 div 加 `min-w-0`、Heading 加 `whitespace-normal break-words`，长 `shortTitle` 在抽屉宽度内可折行不溢出；顺手删遗留 `console.log('event', event)`。另发现 `components/basic/Drawer.tsx:240` 有 `className='xxxxxxxx'` 占位污点（仅声明式 title 路径触发，复合插槽不受影响），待用户确认后清理。lint 0 错误。
- `LiveCalendarEventDrawer.tsx` summary 占位：`event.summary` 为空时原为 `:null` 空白，改为显示占位提示 `t('component.LiveCalendar.drawer.noSummary')`（三语：暂无详情内容 / 暫無詳情內容 / No details available，已补 uiText-zh/zh-Hant/en）。事实澄清：日历 `summary` 完全等于上游 `foresight` 字段（`intelligence_service.py:1820` ← `wallstreetcn_calendar._parse_item:292`），与事件是否发生在未来无关，故「未来事件 summary 都为空」的假设**不成立**。lint 0 错误。
- 重构：详情抽屉挂载逻辑从 LiveCalendarPage 迁至 LiveCalendar 组件内部。LiveCalendar.tsx 新增 `selectedEvent` 状态 + `handleSelectEvent`（内部自管抽屉 + 可选 `onSelectEvent` 外部通知），新增 `countries` prop（Page 经 `useLiveCalendarCountries` 透传，避免重复请求），末尾挂载 `LiveCalendarEventDrawer`。`LiveCalendarProps.onSelectEvent` 由必填改可选。LiveCalendarPage 删除 `selectedEvent` 状态、抽屉 import 与 190-197 挂载段，`<LiveCalendar>` 改透传 `countries`、移除 `onSelectEvent`。lint 0 错误。
- LiveCalendar 组件入参重命名：prop 名 `eventsByDay` → `eventsMap`（按天归格的 Map 用 Map 语义命名更贴合）。涉及组件 props 类型、`LiveCalendar` 解构、内部 `toFullCalendarEvents` 参数与 `useMemo` 引用；Page 调用处属性名改为 `eventsMap={eventsByDay}`（本地变量不变）；`LiveCalendarListView` 自身 prop 名同步改为 `eventsMap`。顺手删除组件内调试残留 `console.log`。lint 0 错误。
- 数据流决策（已向用户澄清）：`eventsMap`（`Map<string, LiveCalendarEventDef[]>`，hook 内 `toDateKeyFromSeconds` 归格一次）是网格视图与 List 视图共享的上游数据源，**不**应把 `toFullCalendarEvents` 产出的 `events`（FullCalendar 的 `EventInput[]`，业务字段压进 `extendedProps.eventDef`，且含周视图 `group-` 归集卡片）传给 `LiveCalendarListView`。原因：① 类型/字段结构不兼容（List 需完整 `LiveCalendarEventDef` 渲染四列）；② `events` 含 List 不需要的聚合卡片；③ 传 `events` 会让 List 内部重复归格。`eventsMap` 省掉的是归格（一次共享），List 内部只做 range 裁剪 + 按日升序 + 组内 startAt 升序（依赖动态视图状态，不可避免）。
- 修复（用户手动）：`useLiveCalendarMonths` 默认 `includeEconomicData` 由 `false` 改为 `true`（`apps/hrs-web/src/hooks/useLiveCalendar.ts:182`），即月度查询默认带 `include_economic_data=true`，后端不再过滤掉 FD 经济数据事件，页面现在能正常显示经济数据。该解构处已补注释（tab/countryId/importanceMin 为客户端过滤；includeEconomicData 默认含 FD）。结论：FD 占比高（当月 519/651），默认隐藏属缺陷，改 true 合理。
- 前端可按 `event.calendarType`（`FE`/`FD`）做客户端筛选（与 includeEconomicData 区分：后者是后端开关决定 FD 是否入库，前者在已含 FD+FE 的数据上裁剪）。实现：在 `UseLiveCalendarOptions` 加 `calendarType?: 'FE'|'FD'`，并在 useMemo 过滤链（`useLiveCalendar.ts:260` 附近）加一层按 calendarType 过滤；页面加「全部/经济数据/大事件」三态切换。需保持 includeEconomicData=true 否则筛不出 FD。用户尚未要求实现。
- **重要事实更正**：不存在「经济数据」分类 Tab（后端 `_CALENDAR_TABS` 仅 all/macro/earnings/ipo/activity）。FD 落库 scope_value='economic_data'，但 `_aggregate_calendar_rows`（intelligence_service.py:1869）把 economic_data 从 tab_keys 剔除，故 FD 的 **tab_keys 恒为空**；`_filter_calendar:1909` 按 tab 过滤时，切到任一具体 Tab（宏观/财报/新股/活动）FD 会被全部过滤。→ **FD 仅在「全部」Tab 下可见，且需 includeEconomicData=true**。此前"切经济数据 Tab 看 FD"的建议是错的，已更正。
- 方案文档 `docs/Live-calendar.md` 同步更新：§3.3（FD 可见条件 + 前端默认 true）、§8.1（点事件 → 抽屉由 LiveCalendar 内部自管）、§8.3（eventsByDay 以 eventsMap prop 传入；includeEconomicData 默认 true 及其影响；hook 名修正为 `useLiveCalendarMonths`、返回字段 `isRefreshing`）、§9.4（目录补 LiveCalendarListView/LiveCalendarEventDrawer/eventTheme；props 改 `eventsMap` + 新增 `countries`/`onRangeChange`，`onSelectEvent` 改可选；骨架补内部自管抽屉与 List 平级渲染）、§18.1/§18.2/§20.3（组件侧 eventsByDay→eventsMap，补 List 不经 toFullCalendarEvents 的理由）、§19（`handleSelectEvent` 为统一开抽屉入口，三视图与 List 共用）。
