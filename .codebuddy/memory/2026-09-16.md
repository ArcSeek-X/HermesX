# 2026-09-16

## 侧栏视觉主题偏好 sidebarTheme 落地（pill/square 可切换 + 持久化）
- 需求：主题设置（ThemeSetting）增加「菜单栏主题模式」切换，逻辑与 themeColor 一致。澄清语义 = `SidebarNavNew` 的 `theme` prop（pill=大圆角胶囊 / square=方角），**非明暗模式**（明暗已全站跟随 themeMode）。
- 方案评审后用户确认执行，采用 **α 预览方案**：draft+保存（与 themeColor 同套路），弹层内缩略示意即时随 draft 切换，真实侧栏保存后随 store 更新（因侧栏在弹层背后，无法像 themeColor 那样改全局 `--primary` 实时 DOM 预览）。
- 改动清单：
  1. `types/theme.ts`：新增 `SidebarTheme = 'pill' | 'square'`（带逐值注释），符合 TYPE_NAMING §单真源。
  2. `SidebarNav-new.tsx`：删本地 `export type SidebarTheme`，改 `import type { SidebarTheme } from '../../../types/theme'` + `export type { SidebarTheme }` re-export（保持对外 API 不变）。
  3. `themeStore.ts`：重写。`sidebarTheme` 状态 + `THEME_SIDEBAR_STORAGE_KEY='theme.sidebarTheme'` + `readStoredSidebarTheme`（白名单校验，回退 `'square'` 与组件默认一致）+ `setSidebarTheme` action（落盘 local）+ `themeReset` 恢复 `'square'`。
  4. `Shell.tsx`：`useThemeStore((s) => s.sidebarTheme)` 订阅，透传 `<SidebarNavNew theme={sidebarTheme} ... />`（替代原不传→默认 square）。
  5. `ThemeSetting.tsx`：弹层新增「侧栏风格」块——标题 `theme.sidebarStyle` + `TabNav`（SIDEBAR_THEMES: pill/square）+ 缩略预览（pill=`rounded-2xl`+primary 边框/阴影，square=`rounded-none`+`border-r`）；`draftSidebarTheme` 草稿，保存 `setSidebarTheme(draftSidebarTheme)`，关闭/重置回退到已保存值。
  6. i18n 三语（zh/en/zh-Hant）：补 `theme.sidebarStyle` / `theme.sidebarPill` / `theme.sidebarSquare`。
- 与 themeColor 链路的两处本质差异（方案评审已指出，落地时确认）：
  - **应用路径**：themeColor 是全局 CSS 变量 → ThemeSync 写 DOM；sidebarTheme 是组件级视觉风格 → **Shell 透传 prop 给 SidebarNavNew，不新增 ThemeSync 逻辑**。
  - **预览机制**：themeColor 预览改 `--primary` 整页即时可见；sidebarTheme 是 prop 驱动、侧栏在弹层背后无法实时 DOM 预览 → 用弹层内缩略示意（α），真实侧栏保存后变。
- 验证：`npx tsc -p tsconfig.json --noEmit` 0 错误（注：根 tsconfig 是 solution 风格，真正全量校验应按文件名过滤，本次自带改动均过）；IDE lint 0 错误。
- 踩坑：Shell 相对路径多一层——`useThemeStore` 初写为 `'../../../stores/themeStore'`（指向 `apps/hrs-web/stores`，不存在），正确应为 `'../../stores/themeStore'`（Shell 在 `components/layout/`，themeStore 在 `src/stores/`）。ThemeSetting 因在 `HeaderComponents/` 多一层，故 `'../../../stores/themeStore'` 才正确。已修。
- docs 演示页 `DocsSideBar.tsx` 未接 store（保持默认 square，方案 Q3 建议不动，已遵循）。

## i18n 主题 key 命名空间重命名（续，同 09-16）
- 把扁平 `theme.*` 主题 key 重组为分层命名空间：`theme.dark/light/system` → `theme.themeMode.dark/light/system`；`theme.menu` → `theme.themeMode.settingTitle`；`theme.primary` → `theme.themeColor.settingTitle`；`theme.sidebarStyle/Pill/Square` → `theme.sidebarTheme.settingTitle/pill/square`。`theme.reset/save/theme/toggle` 不变。
- 同步消费方：`ThemeToggle.tsx`（labelKey + resolveThemeLabel + aria-label）、`ThemeSetting.tsx`（THEME_MODES/SIDEBAR_THEMES 的 labelKey + 各标题 `t(...)`）；三语 zh/en/zh-Hant 定义整块替换。
- 验证：src 全仓搜索旧 key 0 结果；`tsc -p tsconfig.app.json --noEmit` 对改动文件无错；IDE lint 0。
