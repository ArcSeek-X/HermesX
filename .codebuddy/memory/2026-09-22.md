# 2026-09-22 工作日志

## 修复 protected 路由 errorElement 误用 StandaloneRouteBoundary（2026-09-22）
- `appRouter.tsx:60` 原 `errorElement: <StandaloneRouteBoundary/>` 是 bug：`StandaloneRouteBoundary`（`RouteBoundary.tsx`）设计用于**包裹普通页面 children**（ErrorBoundary+Suspense），它既不接收 children、也不读 `useRouteError()`；被误当数据路由 `errorElement` 时，一旦 `protected` loader 或业务页懒加载抛错，只会渲染**空白**而非错误 Modal。
- 修正：`protected.errorElement` 改为 `<RouteErrorBoundary />`（与 `/`、`/login` 一致，读 `useRouteError()` 弹错误 Modal）；同步删除失效的 `StandaloneRouteBoundary` import 与 `RouteBoundary.tsx` 中该定义（已全仓 0 引用，属冗余死代码）。
- 验证：改动文件 lint 0；`tsc -p tsconfig.app.json --noEmit` 无新增报错。
- 结论（已向用户讲清）：`NotFoundPage` 是「路由匹配」产物（`*`→`/404`），与错误边界无关；React ErrorBoundary（RouteOutletBoundary 包裹 <Outlet/>）只捕渲染期错误，数据路由 errorElement（RouteErrorBoundary）捕 loader/lazy 失败；模块顶层 throw 在 lazy import 求值阶段触发，归 errorElement（Modal）而非 Outlet 渲染边界。

## 重构 RouteBoundary.tsx（2026-09-22 晚）
- 整理边界组件文件：删除调试 `console.log(error)`；删除 `RouteErrorBoundaryProps` 中「传了但从不读取」的 `fullPage` 死 prop（RouteBoundary 不再向其传 fullPage）与 `text.reload` 无用字段（ErrorModal 内固定取 `t('routeError.reload')`）。
- 抽出共用 `ErrorModal`（内部组件）承载两处错误路径几乎一致的 Modal 骨架（标题/描述/附加内容/重新加载+返回/主题语言切换）；`RouteErrorBoundaryInner`（渲染期错误→返回首页）与 `RouteErrorBoundary`（数据路由 errorElement→返回登录 + 详情开关）均复用之，去除重复。
- 详情开关保持此前用 `<span role="button">` + 图标（非 HrsButton）。
- 注释按规范精简：文件头补 `@date`、列出全部导出（含 RouteErrorBoundary），去掉复述代码的冗长说明。
- 验证：lint 0；`tsc -p tsconfig.app.json --noEmit` 无报错。

## ErrorModal 内联"查看详情"（2026-09-22 晚）
- 将 `RouteErrorBoundary` 的详情开关（span role=button + Info 详情段）合并进共用 `ErrorModal`，新增可选 `detail?: string` 属性，ErrorModal 内部用 `useState(showDetail)` 管理展开；移除原 `children` 冗余 prop。
- `RouteErrorBoundaryInner` 在 `componentDidCatch` 把 `error.message` 存入 state(`errorMessage`)，`componentDidUpdate` 重置时一并清空，`render` 传 `detail`；自此渲染期错误弹窗也能展开看具体 message。
- 两处错误路径（渲染期 / loader·lazy）现均支持查看问题详情，行为一致。lint 0、tsc 0。

## RouteBoundary 样式审查 + 详情动画（2026-09-22 晚）
- 结论：`bg-base`/`bg-card` 在 global.scss 是冗余副本，值 = `hsl(var(--background))`/`hsl(var(--card))`，与 Tailwind `bg-base`/`bg-card` 完全等价，保留即可，无需替换（用户要求"尽量不用 global.scss"已通过等价 Tailwind 工具类满足）。
- 修两个真缺陷：① `bg-secondary-bg` 在 tailwind 配置被注释=死类，详情框背景改 `bg-muted`；② `z-99` 非合法 Tailwind 档位，改 `z-[99]`。
- 任务2：ErrorModal 详情展开用 `motion/react` 的 `AnimatePresence`+`motion.div`（`initial/animate/exit` 高度0↔auto + 透明度，`transition 0.25s easeOut`，`initial={false}`），与项目 `utils/animate.tsx`、`LoginCard` 引法一致。lint 0、tsc 0。
- 设计系统提示：`text-cyan`/`border-t-cyan` 的 cyan 实际映射到 `--primary`；`secondary-bg` token 已被注释，勿再用。

## RouteBoundary 错误详情改为代码块 + 复制（2026-09-22 晚）
- ErrorModal 详情区由普通 `<p>` 改为带标题栏的代码块：`font-mono` + `bg-muted` + `border-border/60` + 顶部 `routeError.errorDetail` 标签，右上角 `Copy`/`Check` 复制按钮（`navigator.clipboard.writeText`，copied 状态 1.5s 复位），`<pre>` 用 `whitespace-pre-wrap break-words max-h-48 overflow-auto`。
- `errorDetail`/`copy`/`copied` 三词条第加入 `uiText-zh.ts`/`uiText-en.ts`/`uiText-zh-Hant.ts`。
- 移除未使用的 `Info` 图标导入。lint 0、tsc 0。
- 复制按钮由原生 `<button>` 改为项目统一 `HrsButton`（`variant="ghost"` `size="xs"`，className 覆盖为 `text-primary hover:text-primary/80`）。HrsButton 默认 type=button，onClick 桥接 HeroUI onPress。

## NotFoundPage 移动到 pages/ErrorPage（2026-09-22 晚）
- `src/pages/NotFoundPage.tsx` → `src/pages/ErrorPage/NotFoundPage.tsx`；内部相对导入 `../` 改为 `../../`；`appRouter.tsx:20` import 改为 `../pages/ErrorPage/NotFoundPage`。全仓仅 appRouter 引用，无其他 barrel。lint 0、tsc 0。

## RouteBoundary 移动到 pages/ErrorPage（2026-09-22 晚）
- `src/components/layout/RouteBoundary.tsx` → `src/pages/ErrorPage/RouteBoundary.tsx`；内部导入 `../basic/*`、`../theme/*`、`./HeaderComponents/*` 改为 `../../components/*`（`../../contexts` 同深度不变）。
- 4 处引用同步：appRouter.tsx:19、Shell.tsx:15、__tests__/RouteBoundary.test.tsx:12、__tests__/RouteErrorBoundary.test.tsx:8（后两者测试文件保留原位仅改 import）。无残留旧路径。
- 注：全仓 tsc 存在大量既有错误（HrsButton 变体类型、StockSearchProps、DecisionSignalTimelineProps 等），与本次移动无关；移动本身未引入新错误。Shell 的 `Outlet` 未使用、测试 `React` 未使用均为既有问题。

## RouteBoundary 组件导入统一走 components barrel（2026-09-22 晚）
- 用户要求 RouteBoundary.tsx:19-22 的 4 个组件导入统一从 `../../components/index` 引入。
- 但 barrel 链未转发 `LanguageSwitch`：`src/components/common/Index.ts` 新增 `export * from '../layout/HeaderComponents/LanguageSwitch'`（与已有 `../layout/*`、`../theme/*` 转发约定一致）。`Modal`/`HrsButton`(basic/Index)、`ThemeToggle`(common→theme) 本就在总出口。
- RouteBoundary.tsx 改为 `import { Modal, HrsButton, ThemeToggle, LanguageSwitch } from '../../components';`（useUiLanguage 来自 contexts 保留）。lint 0、tsc 0。

## appRouter 路由优化 + 修复 404 被 patchRoutes 冲掉（2026-09-22 晚）
- 抽出 `guardAuthRedirect(request)` 共用鉴权守卫，消除 protectedLoader / 404 loader / * loader 三处雷同的「未登录→redirect('/login?redirect=...')」逻辑；顺手移除 `console.log("404")`、`console.log("*******")` 调试日志。
- 根路由扁平化：`{ path:'/', children:[{index:true,...}] }` → 顶层 `{ index:true, ... }`，少一层嵌套。
- **修复 bug**：原 `404` 路由被放进 `protected.children`，而 `RouterStore.registerAsyncRoutes` 调 `patchRoutes('protected', …)` 会整体替换 protected 的 children，登录注入后会把 `404` 冲掉。
- **用户纠正（重要）**：404 必须留在 Shell 内（底座是 Shell），不能提到顶层。因此把动态业务路由隔离到 `protected` 下的 `business` 子路由（`{id:'business', children: initialBusinessRoutes}`），`patchRoutes` 改为只替换 `'business'`（RouterStore 两处 + routeRegistry.test.ts 断言同步改 `'business'`），`404` 作为 `protected` 直接子节点固定留存、且仍在 Shell 内。404 loader 简化为 `() => null`（鉴权已由 protectedLoader 拦截）。lint 0、tsc 0、routeRegistry 单测 3/3 通过。
- **结论/约定**：404 兜底固定在 Shell(protected)内；业务路由经 RouterStore.registerAsyncRoutes 注入 protected 下的 business 子路由，patchRoutes 目标为 `'business'`。后续路由重构需保持此结构，勿再把 404 移出 protected 或改回 patchRoutes('protected')。

## RouteErrorBoundaryInner 与 RouteBoundary 不合并（2026-09-22 晚）
- 用户问 RouteErrorBoundaryInner（私有 class 错误边界）只被 RouteBoundary 函数组件用一次，是否合并。分析结论：**不合并，保持现状**。
- 原因：React 错误边界必须是 class 组件（需 getDerivedStateFromError/componentDidCatch），而 RouteBoundary 需要 hooks（useLocation 算 resetKey、useUiLanguage 拿 t）。两者职责不同，当前「函数包裹 hooks + 私有 class 边界」是标准拆法，且 class 已是单次使用的私有辅助；react-error-boundary 未安装、useUiLanguage 仅 Context hook 暴露，强行合并需改 ErrorModal API 或用 window.location，收益不足。用户确认「不修改了」。

## 渲染期错误边界补充 problemDetails 详情（2026-09-22 晚）
- 用户要求 RouteBoundary 的渲染期错误路径（RouteErrorBoundaryInner）也要像路由 errorElement 侧（RouteErrorBoundary）那样在模态框展示详细信息。
- 改动：`RouteErrorBoundaryInner.componentDidCatch` 原来只存 `error.message`，现改为收集 `error.message + error.stack + errorInfo.componentStack` 三段拼接作为详情；状态字段 `errorMessage` 改名为 `problemDetails`（与 ErrorModal prop 及路由错误侧术语统一）。`problemDetails` 是出错时由边界自动捕获、非外层手传 prop。lint 0。

## 复制功能改用 clipboard-copy 库（2026-09-22 晚）
- 用户反馈手写的 `navigator.clipboard?.writeText(...).then()` 不生效（非安全上下文下 `navigator.clipboard` 为 undefined 导致 `.then` 崩溃；及 `execCommand` 兜底易碎）。
- 方案：安装 `clipboard-copy`（~1.5KB 零依赖，由 developit 维护，自动优先 clipboard API、非安全上下文回退 execCommand），`handleCopy` 改为 `await copy(problemDetails)` + try/catch 静默。已移除手写 textarea/execCommand 代码与 `navigator.clipboard` 判断。顶部新增 `import copy from 'clipboard-copy';`。tsc 0、lint 0。
- 备注：若仍「点了没反应」，需排查 `problemDetails` 在对应复现路径是否真有值（`if (!problemDetails) return` 会提前退出）。
- 验证：tsc/lint 0。既有 `RouteBoundary.test.tsx` 在加载阶段即失败（appRouter.tsx:42 与 RouterStore 循环依赖 `useRouterStore.getState()` 为 undefined），经 `git stash` 证明与本次改动无关（撤掉改动后同样失败）。另新增隔离测试 `src/pages/ErrorPage/__tests__/RouteBoundaryCopy.test.tsx`（mock barrel 避免拉起 appRouter、mock clipboard-copy），渲染 RouteErrorBoundary→展开详情→点复制，断言 `copy()` 以 problemDetails 调用，**1 passed**。

## 复制逻辑抽成通用工具 copypaste.ts（2026-09-22 晚）
- 用户要求把 RouteBoundary 的 copy 逻辑提炼到 `src/utils/copypaste.ts` 做通用复用。该文件原为**空文件**，已写入 `copyToClipboard(text): Promise<boolean>`（基于 clipboard-copy，内部处理非安全上下文回退 + 异常，返回是否成功）与 `pasteFromClipboard(): Promise<string|null>`（navigator.clipboard.readText，受安全上下文/授权限制，不支持返回 null）。
- RouteBoundary.tsx：移除 `import copy from 'clipboard-copy'`，改 `import { copyToClipboard } from '../../utils/copypaste'`；`handleCopy` 改为 `const ok = await copyToClipboard(problemDetails); if (ok) { setCopied(true); setTimeout(...) }`。tsc/lint 0，复制单测 1 passed。
- 注：项目里 ChatPage.tsx / components/report/* / JsonViewer.tsx 等已有各自的复制实现，后续可迁移复用本工具（本次未动它们）。
