# 前端缓存方案设计

> 版本：v1.1
> 日期：2026-08-01
> 状态：已实施（Phase 1-7 已完成，Phase 8-9 待实施）
> 最后更新：2026-08-01（新增 K 线页面缓存实施记录、useWindowWidth Hook、空值处理缺陷修复）

---

## 一、项目全景分析

### 1.1 应用架构概览

本项目（DSA - Daily Stock Analysis）是一个股票分析系统，包含以下核心模块：

| 路由 | 页面 | 功能描述 | 数据特征 |
|------|------|---------|---------|
| `/` | HomePage | 仪表盘，关键指标概览 | 实时数据，需频繁刷新 |
| `/sector-analysis` | SectorAnalysisPage | 板块分析（概念云图、ETF云图、大盘指标） | 半实时，30秒自动刷新 |
| `/kline` | StockKLinePage | 个股K线图（搜索、多周期K线、技术指标） | 实时行情 + 历史数据 |
| `/chat` | ChatPage | AI 聊天，自然语言交互 | 会话状态，消息历史 |
| `/screening` | StockScreeningPage | 股票筛选，多条件选股 | 筛选条件 + 结果集 |
| `/portfolio` | PortfolioPage | 投资组合管理 | 持仓数据，用户私有 |
| `/decision-signals` | DecisionSignalsPage | 决策信号分析 | 分析结果，图表展示 |
| `/backtest` | BacktestPage | 策略回测 | 回测配置 + 结果 |
| `/alerts` | AlertsPage | 警报规则管理 | 规则CRUD，用户私有 |
| `/usage` | TokenUsagePage | Token 用量统计 | 统计数据 |
| `/settings` | SettingsPage | 系统设置（语言、主题等） | 用户偏好，持久化 |

### 1.2 现有状态管理

| 机制 | 用途 | 存储位置 |
|------|------|---------|
| `AuthProvider` | 用户认证状态 | React Context |
| `UiLanguageProvider` | 语言切换（中/英） | React Context + localStorage |
| `ThemeProvider` | 主题切换（深色/浅色） | React Context + CSS class |
| `agentChatStore` (zustand) | AI 聊天状态、路由追踪 | Zustand store |
| `PageStateStore` (React Context) | 跨路由页面状态保持（L2） | React Context + sessionStorage |
| 各页面内部 `useState` | 页面级临时状态 | 组件内存（路由切换即丢失） |

### 1.3 核心问题

**问题 1：路由切换导致页面状态丢失** ✅ 已解决
- 所有页面使用 React Router 的 `lazy()` 懒加载 + `<Route>` 路由
- 切换路由时，前一个页面组件被卸载，所有 `useState` 状态销毁
- 用户返回时需要重新操作（重新搜索股票、重新设置筛选条件等）
- **解决方案**：引入 `PageStateStore`（L2）+ `useCachedState`（L2+L3）+ `usePreference`（L4）

**问题 2：API 重复请求** ✅ 已解决
- 每次进入页面都重新请求数据，无缓存机制
- 板块分析页面虽然有 30 秒轮询，但切换回来时会触发额外请求
- 股票搜索、K 线数据等高频操作没有去重
- **解决方案**：引入 `apiCache`（L1）+ axios 拦截器自动缓存

**问题 3：用户偏好分散** ✅ 已解决
- 语言偏好通过 `UiLanguageContext` 管理，已持久化到 localStorage
- 主题偏好通过 `ThemeProvider` 管理，已持久化到 CSS class
- 其他用户偏好（如默认周期、默认排序等）没有统一管理
- **解决方案**：引入 `usePreference` Hook 统一管理 L4 用户偏好

**问题 4：聊天会话状态**  待实施
- `agentChatStore` 使用 zustand 管理，但消息历史没有持久化
- 刷新页面后聊天历史丢失

---

## 二、缓存层级设计

基于整个项目的数据特征，设计四级缓存体系：

```
┌─────────────────────────────────────────────────────────
│  L1 - 内存缓存（Map + TTL）                               │
│  用途：API 响应缓存，减少重复请求                            │
│  生命周期：应用运行期间，TTL 过期自动失效                     │
│  实现：utils/apiCache.ts + axios 拦截器                     │
├─────────────────────────────────────────────────────────┤
│  L2 - 页面状态缓存（React Context）                        │
│  用途：跨路由保持页面状态                                    │
│  生命周期：会话期间（切换路由不丢失，刷新页面丢失）             │
│  实现：stores/PageStateStore.tsx                            │
─────────────────────────────────────────────────────────┤
│  L3 - 会话缓存（sessionStorage）                           │
│  用途：页面状态持久化，刷新页面后恢复                         │
│  生命周期：浏览器标签页关闭前                               │
│  实现：hooks/useCachedState.ts（与 L2 联动）                │
├─────────────────────────────────────────────────────────┤
│  L4 - 本地缓存（localStorage）                             │
│  用途：用户偏好设置，永久保存                                │
│  生命周期：永久（用户手动清除）                              │
│  实现：hooks/usePreference.ts                               │
└─────────────────────────────────────────────────────────┘
```

### 2.1 各层级详细说明

#### L1 - 内存缓存（API 响应缓存）✅ 已实施

```typescript
// utils/apiCache.ts

class ApiCache {
  private cache = new Map<string, CacheEntry>();

  get<T>(key: string): T | null;
  set(key: string, data: any, ttlSeconds: number): void;
  invalidate(pattern: string): void;    // 支持通配符清除
  clear(): void;
}

interface CacheEntry {
  data: any;
  expireAt: number;
  createdAt: number;
}
```

**各 API 缓存策略：**

| API 分类 | 接口示例 | TTL | 策略说明 |
|---------|---------|-----|---------|
| **实时行情** | `/kline/{code}/info` | 30 秒 | 价格变化快，短缓存 |
| **K 线数据** | `/kline/{code}/kline` | 60 秒 | 日内数据，中等缓存 |
| **股票搜索** | `/kline/search` | 300 秒 | 股票列表稳定，长缓存 |
| **市场概览** | `/sector/market-overview` | 30 秒 | 与大盘刷新同步 |
| **板块数据** | `/sector/concept-scale` | 300 秒 | 板块变化慢 |
| **ETF 数据** | `/sector/etf-scale` | 300 秒 | ETF 变化慢 |
| **用户数据** | `/portfolio/*`, `/alerts/*` | 0（不缓存） | 用户私有数据，实时获取 |
| **AI 聊天** | `/chat/*` | 0（不缓存） | 对话实时性要求高 |
| **回测结果** | `/backtest/*` | 600 秒 | 计算结果，长缓存 |

#### L2 - 页面状态缓存（跨路由保持）✅ 已实施

```typescript
// stores/PageStateStore.tsx

interface PageStateStore {
  // K 线页面状态
  kline: {
    stockCode: string;
    stockInfo: StockInfo | null;
    klineData: KLinePoint[];
  };

  // 板块分析页面状态
  sector: {
    activeTab: 'concept' | 'etf';
    conceptLimit: number;
    etfLimit: number;
  };

  // 股票筛选页面状态
  screening: {
    filters: ScreeningFilters;
    sortBy: string;
    sortOrder: 'asc' | 'desc';
  };

  // 投资组合页面状态
  portfolio: {
    viewMode: 'list' | 'chart';
    selectedStock: string | null;
  };

  // 决策信号页面状态
  decisionSignals: {
    selectedSignal: string | null;
    dateRange: [string, string];
  };

  // 回测页面状态
  backtest: {
    strategyConfig: StrategyConfig;
    lastResult: BacktestResult | null;
  };

  // 警报页面状态
  alerts: {
    editingRule: AlertRule | null;
    filterStatus: 'all' | 'active' | 'inactive';
  };
}
```

**设计原则：**
- 每个页面只缓存"用户操作产生的状态"，不缓存原始 API 数据
- 原始数据通过 L1 API 缓存获取，页面状态通过 L2 恢复后重新请求（命中 L1 缓存）
- Provider 放在 `App.tsx` 最顶层，确保所有路由共享

**已实施页面：**
- ✅ StockKLinePage（股票代码、股票信息、K线数据）
- ✅ StockScreeningPage（市场、策略、最大结果数）
- ✅ PortfolioPage（选中账户、成本方式）
- ✅ DecisionSignalsPage（重新评估配置）
- ✅ BacktestPage（评估天数、阶段筛选、分析日期、强制重跑）
- ✅ AlertsPage（启用筛选、警报类型筛选）

#### L3 - 会话缓存（刷新恢复）✅ 已实施

```typescript
// hooks/useCachedState.ts

function useCachedState<T>(
  key: string,
  defaultValue: T,
  options?: {
    storage?: 'session' | 'local';
    serialize?: (value: T) => string;
    deserialize?: (raw: string) => T;
  }
): [T, (value: T | ((prev: T) => T)) => void];
```

**使用场景：**
- 页面状态在 L2 的基础上，同步写入 sessionStorage
- 刷新页面时，从 sessionStorage 恢复 L2 状态
- 标签页关闭后，sessionStorage 自动清除

**重要修复（v1.1）：** `useCachedState` 将 `null` 和 `undefined` 都视为无值，触发 fallback 到默认值。详见 9.5 节。

#### L4 - 本地缓存（用户偏好）✅ 已实施

```typescript
// hooks/usePreference.ts

function usePreference<T>(
  key: string,
  defaultValue: T
): [T, (value: T) => void];
```

**已实施偏好项：**
- ✅ 语言偏好：`UiLanguageContext` → localStorage `dsa-language`
- ✅ 主题偏好：`ThemeProvider` → localStorage `dsa-theme`（通过 CSS class）
- ✅ K 线默认周期：`StockKLinePage` → `dsa-pref-kline.period`
- ✅ 板块分析默认 Tab：`SectorAnalysisPage` → `dsa-pref-sector.activeTab`
- ✅ 股票筛选默认条件：`StockScreeningPage` → `dsa-pref-screening.*`
- ✅ 投资组合默认设置：`PortfolioPage` → `dsa-pref-portfolio.*`
- ✅ 决策信号默认设置：`DecisionSignalsPage` → `dsa-pref-decisionSignals.*`
- ✅ 回测默认设置：`BacktestPage` → `dsa-pref-backtest.*`
- ✅ 警报默认筛选：`AlertsPage` → `dsa-pref-alerts.*`

---

## 三、各页面缓存方案详细设计

### 3.1 首页（HomePage）⏳ 待实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 仪表盘数据 | L1 (TTL=60s) | 减少重复请求 |
| 最近查看的股票 | L3 (sessionStorage) | 快速返回上次查看 |

### 3.2 板块分析（SectorAnalysisPage）✅ 已实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 当前 Tab（概念/ETF） | L2 + L4 | 跨路由保持 + 永久记住偏好 |
| 概念数量限制 | L2 | 切换路由后恢复 |
| ETF 数量限制 | L2 | 切换路由后恢复 |
| 市场概览数据 | L1 (TTL=30s) | 30 秒轮询 + 缓存去重 |
| 概念云图数据 | L1 (TTL=300s) | 板块数据变化慢 |
| ETF 云图数据 | L1 (TTL=300s) | ETF 数据变化慢 |

### 3.3 个股 K 线（StockKLinePage）✅ 已实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 选中的股票代码 | L2 + L3 (session) | 切换路由保持 + 刷新恢复 |
| 股票名称（用于输入框显示） | L3 (session) + 派生自 stockInfo | 输入框显示"名称（代码）"格式 |
| 当前周期 | L2 + L4 (local) | 跨路由保持 + 永久记住偏好 |
| 股票实时信息 | L1 (TTL=30s) | 价格实时性高 |
| K 线数据 | L1 (TTL=60s) | 日内数据中等缓存 |
| 搜索历史 | L3 (sessionStorage) | 最近搜索的股票列表 |

**实施细节：**
- 输入框使用 `displayValue` 属性实现"显示值"和"搜索值"分离
- 显示值格式：`中科曙光（603019）`，搜索值：`603019`
- 清除按钮（X）：一键清除所有缓存状态（stockCode、stockName、stockInfo、klineData）
- 中文名称搜索：通过后端 `searchStocks` API 解析为股票代码后再加载

### 3.4 AI 聊天（ChatPage）⏳ 待实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 聊天消息历史 | L3 (sessionStorage) | 刷新页面后恢复对话 |
| 当前对话 ID | L2 | 切换路由后恢复 |
| AI 响应缓存 | 不缓存 | 每次请求都是新的对话 |

### 3.5 股票筛选（StockScreeningPage）✅ 已实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 筛选条件（市场、策略、最大结果数） | L2 + L4 | 跨路由保持 + 永久记住偏好 |
| 排序方式 | L2 | 切换路由后恢复 |
| 筛选结果 | L1 (TTL=60s) | 相同条件不重复请求 |

### 3.6 投资组合（PortfolioPage）✅ 已实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 视图模式（列表/图表） | L2 + L4 | 跨路由保持 + 永久偏好 |
| 持仓数据 | 不缓存 (TTL=0) | 用户私有数据，实时获取 |
| 选中的股票 | L2 | 切换路由后恢复 |

### 3.7 决策信号（DecisionSignalsPage）✅ 已实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 选中的信号类型 | L2 | 切换路由后恢复 |
| 日期范围 | L2 | 切换路由后恢复 |
| 分析结果 | L1 (TTL=300s) | 分析结果变化不频繁 |

### 3.8 策略回测（BacktestPage）✅ 已实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 策略配置 | L2 + L3 | 跨路由保持 + 刷新恢复 |
| 回测结果 | L1 (TTL=600s) | 计算结果，长缓存 |

### 3.9 警报管理（AlertsPage）✅ 已实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 编辑中的规则 | L2 | 切换路由后恢复编辑状态 |
| 筛选状态 | L2 | 切换路由后恢复 |
| 警报规则列表 | 不缓存 (TTL=0) | 用户私有数据，实时获取 |

### 3.10 Token 用量（TokenUsagePage）⏳ 待实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 用量统计数据 | L1 (TTL=60s) | 统计数据，短缓存 |

### 3.11 系统设置（SettingsPage）⏳ 待实施

| 缓存项 | 层级 | 说明 |
|--------|------|------|
| 所有设置项 | L4 (localStorage) | 用户偏好，永久保存 |

---

## 四、技术实现方案

### 4.1 文件结构

```
apps/dsa-web/src/
├── stores/
│   └── PageStateStore.tsx              # 页面状态缓存 Provider（L2）✅
├── utils/
│   ├── apiCache.ts                     # API 响应缓存（L1）✅
│   ── storage.ts                      # 存储工具（封装 localStorage/sessionStorage）✅
├── hooks/
│   ├── useCachedState.ts               # 通用缓存状态 Hook（L2 + L3）✅
│   ├── usePreference.ts                # 用户偏好 Hook（L4）✅
│   └── useWindowWidth.ts               # 窗口宽度监听 Hook（响应式布局）✅ 新增
├── constants/
│   └── cacheConfig.ts                  # 缓存配置（TTL、键名前缀等）✅
├── api/
│   ├── index.ts                        # 修改：集成 L1 缓存拦截器 ✅
│   ├── kline.ts                        # 修改：使用缓存配置 ✅
│   └── sector.ts                       # 修改：使用缓存配置 ✅
├── components/
│   └── StockAutocomplete/
│       └── StockAutocomplete.tsx       # 支持 displayValue + onClear 属性 ✅
└── pages/
    ├── StockKLinePage.tsx              # 缓存实施 + 输入框优化 ✅
    ├── StockScreeningPage.tsx          # 缓存实施 ✅
    ├── PortfolioPage.tsx               # 缓存实施 ✅
    ├── DecisionSignalsPage.tsx         # 缓存实施 ✅
    ├── BacktestPage.tsx                # 缓存实施 ✅
    └── AlertsPage.tsx                  # 缓存实施 ✅
```

### 4.2 核心实现

#### 4.2.1 API 缓存拦截器 ✅

在 axios 拦截器层面统一处理缓存，业务代码无感知：

```typescript
// api/index.ts

import apiCache from '../utils/apiCache';
import { getCacheTTL } from '../constants/cacheConfig';

// 请求拦截器：检查缓存
apiClient.interceptors.request.use((config) => {
  if (config.method === 'get') {
    const cacheKey = buildCacheKey(config);
    const cached = apiCache.get(cacheKey);
    if (cached) {
      // 返回缓存数据（通过 adapter 短路请求）
      config.adapter = () => Promise.resolve({
        data: cached,
        status: 200,
        statusText: 'OK',
        headers: {},
        config,
      });
    }
  }
  return config;
});

// 响应拦截器：写入缓存
apiClient.interceptors.response.use((response) => {
  if (response.config.method === 'get') {
    const cacheKey = buildCacheKey(response.config);
    const ttl = getCacheTTL(response.config.url);
    if (ttl > 0) {
      apiCache.set(cacheKey, response.data, ttl);
    }
  }
  return response;
});
```

#### 4.2.2 页面状态 Provider ✅

```typescript
// stores/PageStateStore.tsx

const PageStateContext = createContext<PageStateStore | null>(null);

export const PageStateProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [state, setState] = useState<PageStateStore>(() => {
    // 从 sessionStorage 恢复状态
    return restoreFromSessionStorage() ?? getDefaultState();
  });

  // 状态变化时同步到 sessionStorage
  useEffect(() => {
    saveToSessionStorage(state);
  }, [state]);

  return (
    <PageStateContext.Provider value={{ state, setState }}>
      {children}
    </PageStateContext.Provider>
  );
};
```

#### 4.2.3 通用缓存 Hook ✅（含空值修复）

```typescript
// hooks/useCachedState.ts

export function useCachedState<T>(
  key: string,
  defaultValue: T,
  options?: { storage?: 'session' | 'local' }
): [T, (value: T | ((prev: T) => T)) => void] {
  const { state, setState: setGlobalState } = usePageState();
  const storage = options?.storage;

  // 从全局状态读取（null/undefined 视为无值，使用默认值）
  let currentValue: T = defaultValue;
  try {
    const moduleState = state[moduleKey] as unknown as Record<string, unknown>;
    const nested = path.length > 1 ? getNestedValue(moduleState, path.slice(1)) : moduleState;
    if (nested !== undefined && nested !== null) {
      currentValue = nested as T;
    }
  } catch { /* 读取失败时使用默认值 */ }

  // 如果 PageStateStore 中没有有效值，尝试从浏览器存储恢复（L3）
  if (currentValue === defaultValue && storage) {
    try {
      const stored = getStorageItem<string>(key, storage);
      if (stored !== null) {
        currentValue = deserialize(stored);
      }
    } catch { /* 恢复失败时使用默认值 */ }
  }

  // 更新函数：同时更新 PageStateStore 和浏览器存储
  const setValue = useCallback((value: T | ((prev: T) => T)) => {
    const newValue = typeof value === 'function'
      ? (value as (prev: T) => T)(currentValue)
      : value;

    // 更新 PageStateStore（L2）
    if (path.length === 1) {
      setGlobalState(moduleKey, newValue as any);
    } else {
      const moduleState = state[moduleKey] as unknown as Record<string, unknown>;
      const updated = setNestedValue(moduleState, path.slice(1), newValue);
      setGlobalState(moduleKey, updated as any);
    }

    // 持久化到浏览器存储（L3）
    if (storage) {
      try {
        setStorageItem(key, serialize(newValue), storage);
      } catch { /* 存储失败时静默处理 */ }
    }
  }, [key, path, moduleKey, currentValue, state, setGlobalState, storage, serialize]);

  return [currentValue, setValue];
}
```

#### 4.2.4 窗口宽度监听 Hook ✅ 新增

```typescript
// hooks/useWindowWidth.ts

export function useWindowWidth(debounceMs = 150): number {
  const [width, setWidth] = useState(getWindowWidth);
  const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  const handleResize = useCallback(() => {
    if (timerRef.current !== null) {
      clearTimeout(timerRef.current);
    }
    timerRef.current = setTimeout(() => {
      setWidth(getWindowWidth());
    }, debounceMs);
  }, [debounceMs]);

  useEffect(() => {
    window.addEventListener('resize', handleResize);
    return () => {
      window.removeEventListener('resize', handleResize);
      if (timerRef.current !== null) {
        clearTimeout(timerRef.current);
      }
    };
  }, [handleResize]);

  return width;
}
```

**使用场景：**
- 响应式布局：根据窗口宽度切换组件渲染策略
- 图表适配：小屏幕时隐藏刻度、调整间距
- 移动端检测：宽度小于阈值时启用移动端样式

### 4.3 App.tsx 改造 ✅

```typescript
// App.tsx

import { PageStateProvider } from './stores/PageStateStore';

const App: React.FC = () => {
  return (
    <UiLanguageProvider>
      <Router>
        <AuthProvider>
          <PageStateProvider>          {/* ← 新增：页面状态缓存 */}
            <AppContent />
          </PageStateProvider>
        </AuthProvider>
      </Router>
    </UiLanguageProvider>
  );
};
```

---

## 五、缓存键名规范

### 5.1 命名规则

```
格式：{模块}:{资源}:{标识}

示例：
  kline:info:603019          # 中科曙光实时信息
  kline:kline:603019:daily   # 中科曙光日K线
  sector:overview            # 市场概览
  sector:concept:100         # 概念云图（前100）
  sector:etf:50              # ETF云图（前50）
  screening:result:filters   # 筛选结果
  backtest:result:strategy1  # 回测结果
```

### 5.2 存储键名前缀

```
localStorage:   dsa-pref-{key}     # 用户偏好（L4）
sessionStorage: dsa-state-{key}    # 页面状态（L3）
```

---

## 六、缓存清除策略

### 6.1 自动清除

| 触发条件 | 清除范围 | 说明 |
|---------|---------|------|
| TTL 过期 | 对应 L1 缓存项 | 自动失效，下次请求重新获取 |
| 标签页关闭 | L3 sessionStorage | 浏览器自动清除 |
| 用户登出 | 全部 L1/L2/L3 | 清除所有会话数据 |
| 手动刷新 | 对应 L1 缓存项 | 清除后重新请求 |

### 6.2 手动清除

| 操作 | 清除范围 | 入口 |
|------|---------|------|
| 清除 API 缓存 | 全部 L1 | 设置页面 / 开发者工具 |
| 清除页面状态 | 全部 L2/L3 | 设置页面 |
| 清除用户偏好 | 全部 L4 | 设置页面 → "恢复默认设置" |
| 清除全部 | L1+L2+L3+L4 | 设置页面 → "清除所有数据" |

### 6.3 智能失效

```typescript
// 当用户修改警报规则时，自动清除相关缓存
function invalidateRelatedCache(entity: string, id: string) {
  apiCache.invalidate(`${entity}:*:${id}`);
  apiCache.invalidate(`${entity}:list`);
}
```

---

## 七、性能预估

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 切换路由返回时间 | 2~5 秒（重新加载） | < 100ms（状态恢复） | **20~50x** |
| API 请求次数（典型会话） | ~50 次/小时 | ~15 次/小时 | **减少 70%** |
| 页面刷新恢复时间 | 3~8 秒（全部重新加载） | 1~2 秒（从缓存恢复） | **3~4x** |
| 内存占用增加 | - | ~5~10 MB | 可接受 |

---

## 八、实施计划与进度

| 阶段 | 内容 | 涉及文件 | 预计工时 | 状态 |
|------|------|---------|---------|------|
| **Phase 1** | 基础设施：`apiCache.ts` + `storage.ts` + `cacheConfig.ts` | utils/, constants/ | 0.5 天 | ✅ 已完成 |
| **Phase 2** | API 拦截器集成：修改 `api/index.ts` | api/index.ts | 0.5 天 | ✅ 已完成 |
| **Phase 3** | 页面状态 Provider：`PageStateStore.tsx` + `useCachedState.ts` | stores/, hooks/ | 1 天 | ✅ 已完成 |
| **Phase 4** | 改造 K 线页面 | StockKLinePage.tsx | 0.5 天 | ✅ 已完成 |
| **Phase 5** | 改造板块分析页面 | SectorAnalysisPage.tsx | 0.5 天 | ✅ 已完成 |
| **Phase 6** | 改造聊天页面（消息历史持久化） | ChatPage.tsx, agentChatStore.ts | 0.5 天 | ⏳ 待实施 |
| **Phase 7** | 改造其他页面（筛选、组合、信号、回测、警报） | 对应页面文件 | 1 天 | ✅ 已完成 |
| **Phase 8** | 设置页面：缓存管理功能 | SettingsPage.tsx | 0.5 天 |  待实施 |
| **Phase 9** | 测试验证 + 边界情况处理 | 全局 | 0.5 天 | ⏳ 待实施 |

**已完成：约 4.5 天 / 总计：约 5.5 天**

---

## 九、风险与注意事项

### 9.1 内存泄漏风险
- L1 Map 缓存需设置最大条目数（建议 200 条），超出时 LRU 淘汰
- 组件卸载时需清理订阅

### 9.2 数据一致性风险
- 用户私有数据（portfolio/alerts）不缓存或极短缓存，避免显示过期数据
- 写操作（POST/PUT/DELETE）后自动清除相关 L1 缓存

### 9.3 存储空间风险
- sessionStorage 限制约 5~10 MB，需控制存储量
- 大对象（如 K 线数据）只存 L1 内存缓存，不写入 sessionStorage
- sessionStorage 只存储轻量状态（股票代码、周期、筛选条件等）

### 9.4 兼容性
- 所有缓存取放需 try-catch，防止私有模式下 localStorage 不可用
- SSR 环境下需判断 `typeof window !== 'undefined'`

### 9.5 useCachedState 空值处理缺陷（已修复）

**问题描述**：`useCachedState` 在读取 PageStateStore 时，只判断 `nested !== undefined`，未将 `null` 视为"无值"。当外部通过 `setPageState` 将某字段设为 `null` 时，`useCachedState` 不会 fallback 到默认值，导致组件显示异常（如输入框显示字符串 "null"）。

**触发场景**：
```typescript
// onClear 中同时调用 setStockCode('') 和 setPageState({ stockCode: null })
// setPageState 覆盖了 setStockCode 的设置，stockCode 变为 null
// useCachedState 读取到 null，不 fallback 到默认值 ''
// 输入框 value={null} → 显示 "null"
```

**修复方案**：
1. `useCachedState` 中将 `null` 和 `undefined` 都视为无值，触发 fallback 到默认值
2. 清除操作时，`setPageState` 中的字段值应与 `useCachedState` 的默认值保持一致（如 `stockCode: ''` 而非 `stockCode: null`）

**最佳实践**：
- `useCachedState` 的 `defaultValue` 应与 `setPageState` 中对应字段的清除值保持一致
- 避免在同一操作中混用 `setStockCode('')` 和 `setPageState({ stockCode: null })`
- 清除时优先使用 `setPageState` 一次性更新所有相关字段，避免多次 setState 导致状态覆盖冲突

### 9.6 多次 setState 状态覆盖冲突（已修复）

**问题描述**：在同一个回调函数中，先调用 `setStockCode('')`（通过 `useCachedState` 更新 PageStateStore），再调用 `setPageState('kline', { ...pageState.kline, stockCode: null })`。由于 `setPageState` 使用的是闭包中捕获的旧 `pageState`，会覆盖 `setStockCode` 的更新。

**修复方案**：
- 清除操作时，`setPageState` 中的字段值应与 `useCachedState` 的默认值保持一致
- 避免在同一操作中混用两种 setState 方式设置同一字段

---

## 十、待确认事项

- [x] L1 缓存最大条目数（建议 200）→ 已确认
- [ ] 聊天消息历史是否需要永久保存（当前方案为会话级）
- [ ] 是否需要支持 IndexedDB 存储大量历史数据
- [ ] 缓存统计和监控是否需要（开发者工具中查看缓存命中率）
- [ ] 离线支持是否需要（Service Worker + Cache API）
