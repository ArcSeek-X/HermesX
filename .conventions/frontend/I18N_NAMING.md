# &#x20;i18n 多语言管理规范

> 适用范围：`apps/hrs-web/src/i18n/` 三语主字典（`uiText-zh.ts` / `uiText-zh-Hant.ts` / `uiText-en.ts`，经 `uiText.ts` 聚合为 `UI_TEXT`，由 `UiLanguageContext.t()` 消费）。
> 本规范对**新增文案强制生效**；存量 key 不统一改名，通过「登记表 + 渐进迁移」管理。

## 1. 总则

1. 全站界面文案必须走 i18n 字典，禁止在组件中硬编码用户可见文案。
2. `uiText-zh.ts` 是所有 key 的**真源（source of truth）**，新增/删除 key 必须三语文件同步修改（类型系统会强制校验）。
3. 繁体必须是**真繁体**，不允许用简体或拼音替代。
4. 每个 key 在字典文件中必须附行尾中文注释，说明用途。
5. 新增 key 前必须先检索现有 key，禁止同义重复（如已有 `common.cancel`，不得再造 `common.cancelBtn`）。
6. 每个 key 在字典文件中必须附行尾中文注释，说明用途。
7. 加入到全局 commo的内容必须严谨，必要情况下，需要二次确认，以免污染全局

## 2. 七大类别与 key 前缀契约

| # | 类别        | key 前缀                           | 覆盖范围                                                                 | 现状                                                                           |
| - | --------- | -------------------------------- | -------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| 1 | 全局 common | `common.**********`              | 通用动作/状态/占位符：取消、确认、加载、暂无数据、重试等普适文案                                    | 已有 25 key，合规                                                                 |
| 2 | 语言类别      | `language.`                      | 语言切换、语言名称、语言简写                                                       | 已有 9 key，合规                                                                  |
| 3 | 主题类别      | `theme.`                         | 主题切换、主题模式、主色设置                                                       | 已有 9 key，合规                                                                  |
| 4 | 布局与菜单     | `layout.`、`header.`              | 侧边栏、导航框架、一二级菜单（`layout.nav.`）、路由标题/描述（`layout.route.`）、头部操作区\*\*\*\* | 已有 60 key，合规                                                                 |
| 5 | 登录/注册页    | `auth.login.`、`auth.register.**` | 登录页、初始密码设置页、注册页（预留）                                                  | 存量 `login.` 25 key，登记为存量                                                     |
| 6 | 组件类别      | `component.<组件注册名>.<具体内容>`       | 基础组件、业务组件、通用组件的内部文案                                                  | 新增强制；存量 `taskPanel.`、`runFlow.`、`stockBar.`*\`\`*`******`\* 等登记为存量\*\*\*\*\* |
| 7 | 页面类别      | `<路由名>.<一级功能>[.<二级>]`            | 各页面专属文案                                                              | 存量已基本合规（`kline.`、`usage.****` 等）\*\*\*\*                                     |

## 3. 命名细则

### 3.1 通用规则

- 分隔符统一为 `.`；每段使用 camelCase\*\*（数字/缩写保留原样，如 `1m`、`5d`）。\*
- **key 层级**上限 4 段\*\*。\*\*
- 末段描述具体语义，推荐使用固定词表：`title` / `description` / `placeholder` / `hint` / `emptyTitle` / `emptyDescription` / `errorTitle` / `loading` / `submit` 等。

### 3.2 页面类别（第 7 类）

- 路由名 = 路由 path 段的 camelCase，一段路由对应一个前缀。
- 一级：`<路由名>.<一级>`，例：`kline.title`
- 二级：`<路由名>.<一级>.<二级>`，例：`kline.period.1m`
- 页面专属文案必须挂页面命名空间，不得散落到 `common.`。

**路由 → key 前缀对照表（举例）：**

| 路由前缀                        | 页面                |
| --------------------------- | ----------------- |
| `home.*******`              | 首页                |
| `review.`                   | 复盘（每日选股分析）        |
| `watchlist.`                | 自选                |
| `kline.`                    | 个股 K 线            |
| `usage.****`                | 用量                |
| `settings.`                 | 设置                |
| `chat.`                     | 问股                |
| `decisionSignals.`          | AI 建议             |
| `sectorAnalysis.****`       | 板块分析              |
| `alerts.`                   | 告警                |
| `backtest.`                 | 回测                |
| `portfolio.`                | 持仓                |
| `screening.****`            | 选股                |
| `stockCloud.`               | 个股云图              |
| `codeTest.`                 | 测试页               |
| `notFound.` / `routeError.` | 404 / 路由错误（框架级页面） |

### 3.3 组件类别（第 6 类）

- 格式：`component.<组件注册名>.<具体内容>`。
- 组件的导出/注册名（PascalCase）的理解，举例：挂载文件：components/basic/Index.ts中，“export from './Modal';”，注册名就为“ Modal。
- 完整示例举例：`component.RunFlowDrawer.drawerTitle`、`component.TaskPanel.title`。
- 仅被单一组件使用的文案挂组件命名空间；被 ≥2 个页面/组件复用的文案，上提到 `common.`（普适语义）或抽为独立组件命名空间。注意：为了确保`common.`的纯粹性，上提到 `common.需要确认审核`

### 3.4 登录/注册类别（第 5 类）

- \*\**登录与初始密码设置：`auth.login.`*
- 现存 `login.`*\`\`*`******`\* ，需要改成\*\*`auth.login.`\*\*

***

- *注册设置：`auth.register.`*（当前页面无实现，前缀预留，实现时直接按新规范落地）

## 4. 字典边界

| 字典            | 位置                                               | 语言                            | 适用内容                  |
| ------------- | ------------------------------------------------ | ----------------------------- | --------------------- |
| 主字典 `UI_TEXT` | `src/i18n/uiText-.ts******`                      | zh / zh-Hant / en 三语齐全        | 全站通用 UI 文案（本规范七大类）    |
| 业务字典          | `src/locales/featureText.ts`、`settingsHelp.ts` 等 | 仅 zh / en，繁体经 `toCnOrEn()` 回退 | 告警/回测/组合等业务域文案、配置帮助文本 |

规则：界面框架与通用交互文案进主字典；强业务域、且繁体需求弱的文案可留在业务字典，但新增业务字典必须登记在本规范第 6 节。

## 5. 新增文案流程

### 5.1 *判定类别（决策树）：*

- 是否全站普适（动作/状态/占位符）？→ `common.`，注意：为了确保`common.`的纯粹性，上提到 `common.需要确认审核`
- 是否语言/主题/布局菜单/头部相关？→ 对应第 2/3/4 类
- *是否登录注册链路？→* *`auth.`*`****`
- **是否仅某一组件内部使用？→** **`component.<注册名>.`**

* *以上都不是 → 页面类别* *`<路由名>.`*``**`*``

**查重**：全局搜索候选语义，禁止重复新增。

### 5.2 **三语同改**\*：\*

- **三语同改**：`zh` / `zh-Hant` / `en` 三份文件补同一 key，附行尾注释；缺任何一语类型编译即报错。
- `**模板参数**：占位符统一 ` {name} `形式（`formatUiText`契约）；中英文量词/单位差异在各自语言文件内处理（如`{count}只`vs`{count} stocks`），不允许把单位写死在组件里。`

## 6. 存量登记表（需要实时更新）

| 命名空间                                                                                                                                                                                                                                                                                 | key 数                                                            | 归属类别  | 合规性                             |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | ----- | ------------------------------- |
| common.`/`language.\*\*\**\`\`\*\*`/`theme.`****`*                                                                                                                                                                                                                                   | 25 / 9 / 9                                                       | 1/2/3 | 合规，保持不动                         |
| layout.**\`\`**`/`header.`****`                                                                                                                                                                                                                                                      | 57 / 3                                                           | 4     | 合规，保持不动                         |
| login.**\`\`**                                                                                                                                                                                                                                                                       | 25                                                               | 5     | 存量，未来重构时迁至 auth.login.\`\`      |
| taskPanel.`/`runFlow.`/`stockBar.\*\**\`\`*                                                                                                                                                                                                                                          | 14 / 106 / 5                                                     | 6     | 存量，未来重构时迁至 component.<注册名>.\`\` |
| kline.`/`usage.`/`review.`**```*` / `decisionSignals.`\* / *`*watchlist.```**```*`* */* *`settings.`* / *`*chat.```**```*`* */* *`sector.`* / *`*history.```**```*`* */* *`stockTrend.`* / *`*report.```**```*`* */* *`routeError.`* / *`*notFound.``***\`\`\*\*`/`stockUnit.`****`* | 43 / 36 / 54 / 206 / 38 / 275 / 25 / 9 / 19 / 32 / 3 / 4 / 4 / 4 | 7     | 合规（页面/页面内组件域），保持不动              |

渐进迁移约定\*\*：不为改名单独发 PR；当存量文件发生实质性重构时，可顺带迁移该文件的文案并全量替换调用点。

## 7. 验证与守护

- `****现有硬约束（保留）**：**`**UI\_TEXT: Record\<UiLanguage, Record\<UiTextKey, string>>\` 类型强制三语 key 对齐。**
- **评审约定**：PR review 时检查新增 key 的类别归属、层级上限、注释齐全、三语齐全。
- \*\**后续事项（本规范不实现）*：在 web-gate 中补充「未使用 key 检测」脚本，定期清理死 key。

# 8. 版本改动记录

| 版本     | 更新时间         | 更新内容                                |
| :----- | :----------- | :---------------------------------- |
| V1.0   | `2026-08-28` | z首次定稿，确立七大类别、页面/组件命名契约、存量登记表与渐进迁移约定 |
| <br /> | <br />       | <br />                              |

