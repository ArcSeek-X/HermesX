/**
 * @file index.ts
 * @description ScrollShadow 组件子目录的统一出口：集中 re-export 组件实例与公开类型，
 * 供上层通过 `src/components/basic` 的 barrel 聚合后统一消费。
 * @author Lensgcx (GaoCangxiong)
 */
export * from './ScrollShadow';
